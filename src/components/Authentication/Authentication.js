import React    from "react";
import template from "./Authentication.js";

class Authentication extends React.Component {
  render() {
    return template.call(this);
  }
}

export default Authentication;
